const logVisibleLightWaves = () => {
    const lightWaves = 'Moonlight';
    console.log(lightWaves);
}

logVisibleLightWaves();
console.log(lightWaves);

//Learned that variables that are within a block are only accessable within that block or locally.